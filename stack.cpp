#include <bits/stdc++.h>
using namespace std;


int main() {
    int num = 1;
    // vector 模拟栈
    {
        vector<int> st;
        st.push_back(num);
        int top_element = st.back();
        st.pop_back();
    }
}
